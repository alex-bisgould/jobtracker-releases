# Job Tracker — releases

Auto-update feed for the Job Tracker desktop app (electron-updater).

This repository holds ONLY the packaged release artifacts (`.dmg`, `.zip`, blockmaps, and `latest-mac.yml`). The application source lives in a separate private repository.

Releases are published automatically by electron-builder during `npm run app:build -- --publish always`.
